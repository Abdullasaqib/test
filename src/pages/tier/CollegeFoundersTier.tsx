import { Navigate } from "react-router-dom";

// Redirect to Coming Soon waitlist page
const CollegeFoundersTier = () => {
  return <Navigate to="/college-founders" replace />;
};

export default CollegeFoundersTier;
